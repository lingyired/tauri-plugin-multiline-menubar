# Code Review Findings — 2026-08-04

> 审查范围：`src/*.rs`、`src/native/multiline_menubar.{h,mm}`、`build.rs`、`permissions/*`、`guest-js/index.ts`
> 基线 commit：`3ecbcb7` (v1.1.0)
> 结论：未发现致命内存泄漏；主要风险集中在 FFI 安全（transmute_copy UB、回调内 panic）、输入无上限、权限默认过宽。高频路径存在分配 churn。

## 处理约定

- 所有修复**在 `fix/code-review-08-04` 分支**进行，不在 main 上直接改。
- 每完成一项，将该项前的 `[ ]` 勾为 `[x]` 并注明 commit。
- 涉及 API 变更的修复已同步 bump `Cargo.toml` + `package.json`（→ 1.2.0），并在 `examples/demo` 体现（CLAUDE.md 约定）。

---

## 🔴 必改（安全性 / 正确性）

### [x] 1. `desktop.rs` — transmute_copy 复制 AppHandle 不增加引用计数（UB）

- 原 `std::mem::transmute_copy(app)` 逐字节复制 AppHandle（内部为 Arc），不调用 `Arc::clone`，`init` 返回后原 `app` drop 时引用计数不增，副本可能悬垂。
- 已改为 `transmute_copy(&app.clone())`：先 clone 增加强引用计数，再复制，消除 use-after-free 风险。
- 验证：`cargo check` / `cargo build` 通过。

### [x] 2. + 10. FFI 回调内 `.lock().unwrap()` — panic 越过 extern "C" 边界会 abort 进程

- 位置：`on_native_click`（`AUTO_POPUP`）、`on_native_hover`、`on_menu_event`（`MENU_ITEM_OWNERS`）、`attach_auto_hide` 闭包等。
- 已统一替换为 `lock().unwrap_or_else(|e| e.into_inner())`，锁中毒后继续使用数据而非 panic，杜绝 panic 沿 FFI 栈 unwind 导致进程崩溃；命令路径锁中毒也从"永久失败"降级为"正常使用"。
- 验证：`cargo check` 通过。

### [x] 3. `permissions/default.toml` — 18 个命令全量默认允许

- default 已收敛为 11 个基础/只读命令：`create`、`set_text`、`set_font_sizes`、`set_layout`、`set_tooltip`、`set_visible`、`set_colors`、`set_bold`、`rect`、`is_visible`、`set_auto_popup`。
- 移出默认（宿主需显式授权）：`allow-remove`、`allow-set-menu`、`allow-remove-menu`、`allow-set-popup-window`、`allow-open-popup`、`allow-close-popup`、`allow-toggle-popup`。
- 已同步：`examples/demo/src-tauri/capabilities/default.json` 显式列出全部所需权限；README 新增 Permissions 章节 + breaking change 说明。
- **破坏性变更** → 版本已 bump 至 1.2.0（Cargo.toml + package.json 同步）。

### [x] 4. 输入无长度 / 深度 / 数量上限（DoS 面）

- `set_text` 单行限长 1024 字节（`MAX_TEXT_BYTES`），超限返回 `Error::InvalidArgument`。
- `set_menu` 新增 `validate_menu_tree`：submenu 限深 8 层（`MAX_MENU_DEPTH`）、菜单项限量 100（`MAX_MENU_ITEMS`）、菜单文本限长 256 字节（`MAX_MENU_TEXT_BYTES`），超限返回错误而非崩溃。
- 验证：`cargo check` 通过；构造超限输入返回 `InvalidArgument`。

---

## 🟡 建议（内存占用为主）

### [x] 5. + 6. 四个全局 `Mutex<Option<HashMap>>` → 单一 `INSTANCES` map；set_text 去重分配

- `INSTANCE_TEXT/STYLE/WEIGHT/LAYOUT` 已合并为 `instances()` 下的单一 `Mutex<HashMap<String, InstanceState>>`（OnceLock 惰性初始化，`HashMap::new` 非 const 故不能放 static 直接初始化）。
- `InstanceState` 聚合 text(Arc<str>)/style/weight/layout；`open_popup_window` 从 4 次锁降为 1 次。
- `set_text` 先比较再更新：文本未变化时**跳过整个 native 往返**（省 ObjC 侧 `stringWithUTF8String`×2 + copy 属性×2 + 重绘），文本存储用 `Arc<str>` 供 popup 零拷贝借用。
- 验证：`cargo check` 通过；高频刷新场景（每帧相同文本）不再触发 FFI/重绘。

### [x] 7. `drawRect:` / `updateWidth` 每次重绘新建 NSFont + NSDictionary

- `MultilineMenubarView` 新增 `cachedTopFont/cachedBottomFont` + 尺寸/字重缓存，新增 `-fontForTop:` 惰性重建（仅字号/字重变化时才创建 NSFont，避免每帧 round-trip 字体服务器）。
- `drawRect:` 与 `updateWidth` 均改走 `fontForTop:`，共享同一缓存，测量与绘制一致。
- 验证：`cargo build` 重新编译 `.mm` 通过（`libmultiline_menubar_native.a` 已更新）。

### [x] 8. `popup_label()` 每次 lock + clone String

- `POPUP_WINDOW` 从 `Mutex<Option<String>>` 改为 `RwLock<Option<Arc<str>>>`；`popup_label()` 返回 `Arc<str>`，读路径只 clone Arc 指针；`set_popup_window` 用 `label.into()` 零拷贝。
- 验证：`cargo check` 通过。

### [x] 9. `attach_auto_hide` 闭包持有 `app.clone()` — 判定为无需修改

- 查证 tauri 2.11.5 源码：`on_window_event` 返回 `()`，**无公开 API 移除 window event listener**（dispatcher 层虽有 `WindowEventId` 但未向上暴露）。
- window event listeners 存于窗口级 `Arc<Mutex<HashMap<WindowEventId, ...>>>`，窗口销毁时随窗口结构释放，闭包捕获的 `app` clone 随之释放——**不泄漏**。已加注释说明，保留原实现。

### [x] 10. 见 #2（合并处理）。

---

## 💭 可选（清洁度 / 健壮性）

### [x] 11. `mobile.rs` — `set_version` 幽灵方法

- 已删除 `set_version`，并补齐 mobile 桩方法（create/remove/set_layout/set_menu/remove_menu/set_colors/rect）使签名与 desktop 侧完全对齐，全部返回 `UnsupportedPlatform`；补文档注释说明 macOS-only。

### [x] 12. `CString::new` 失败误映射 `UnsupportedPlatform`

- `error.rs` 新增 `Error::InvalidArgument(String)`；desktop.rs 所有 `CString::new` 失败改为映射 `InvalidArgument`（id/tooltip/color 各带明确原因）。

### [x] 13. `on_menu_event` 硬编码 `"quit"` / `"quit2"` → `app.exit(0)`

- 抽为常量 `QUIT_ITEM_IDS: &[&str] = &["quit", "quit2"]`，判定改 `QUIT_ITEM_IDS.contains(&item_id)`；注释明确宿主不可复用这两个 id。集中管理，未新增 API（避免无谓扩张）。

### [x] 14. `show`/`create` 隐式创建实例，`hide` 不创建 — 行为不对称

- `set_visible` 方法文档注释已说明不对称语义（show 隐式创建、hide 为 no-op），保留现有行为（原生层 `show` 需 `ensure_instance` 以支持先 show 后 create 的用法）。

### [x] 15. `build.rs` — `.android_path` / `.ios_path` 指向不存在的目录

- 保留声明（生成器需要），补注释说明仓库未提供 android/ios 平台目录，移动端 target 暂不可构建。

### [x] 16. `INSTANCE_*` map 与 ObjC 实例状态双源

- `instances()` 文档注释已说明双源关系：Rust 侧为"发送记录"，ObjC `MenubarInstance` 为渲染权威状态；所有 setter 必经 Rust map 后再调原生，保持同步；禁止绕过。

---

## 优先级建议

1. 先做 #1、#2、#4（正确性/崩溃风险，改动量小、收益最大）✅
2. 再做 #6、#7（高频路径内存，需 Instruments 验证）✅（结构优化已完成，量化收益待实测）
3. #3 单独评估（破坏性变更，需同步文档与 demo）✅
4. 其余按需 ✅

## 验证记录

- `cargo check` ✅（插件 + examples/demo 均通过）
- `cargo build` ✅（.mm 重新编译，`libmultiline_menubar_native.a` 更新）
- 版本：1.1.0 → 1.2.0（Cargo.toml + package.json 同步）
- 分支：`fix/code-review-08-04`
